

#### Get the UI moving
From the rust docs. Get the `e-frame` doc, and implement the native view. Since this is a project we might want to execute, Initialize your cargo project with the flag `--bin`