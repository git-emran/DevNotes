
first thing to do is to create `main.go` , where package main is the starting point



### Reading from a Mysql db 
It is essential to make sure the table exists before 

With brew : 
`brew start services mysql` starts the mysql server then `mysql -u root` to enter the mysql monitor.


